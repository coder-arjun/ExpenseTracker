@model ExpenseTracker.Models.Domain.Saving

@{
    ViewData["Title"] = "Delete Saving";
}

<div class="page-header">
    <div class="eyebrow mb-2"><span class="seal-dot">&#9679;</span> Remove saving</div>
    <h1><i class="bi bi-trash me-2"></i>Delete Saving</h1>
    <p><a asp-action="Index" class="text-decoration-none">Savings</a> / Delete</p>
</div>
<div class="delete-warning">
    <i class="bi bi-exclamation-triangle-fill"></i>
    Are you sure you want to delete this saving? This action cannot be undone.
</div>
<div class="row">
    <div class="col-md-6">
        <div class="card">
            <div class="card-body">
                <dl class="row mb-0">
                    <dt class="col-sm-4">Amount</dt>
                    <dd class="col-sm-8 text-tabular t-balance fw-semibold">@Model.Amount.ToString("C")</dd>
                    <dt class="col-sm-4">Date</dt>
                    <dd class="col-sm-8 folio">@Model.Date.ToString("dd MMM yyyy")</dd>
                    <dt class="col-sm-4">Note</dt>
                    <dd class="col-sm-8">@Model.Note</dd>
                    <dt class="col-sm-4">Month</dt>
                    <dd class="col-sm-8">@Model.YearMonth</dd>
                </dl>
            </div>
        </div>
    </div>
</div>
<div class="mt-3">
    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <button type="submit" class="btn btn-danger"><i class="bi bi-trash me-1"></i>Delete</button>
        <a asp-action="Index" class="btn btn-outline-secondary ms-2">Cancel</a>
    </form>
</div>
